import { ReactNode } from "react";

export const metadata = {
  title: "Create new account",
  description: "Create account",
};

const CreateAccountLayout = ({ children }: { children: ReactNode }) => {
  return <>{children}</>;
};

export default CreateAccountLayout;
